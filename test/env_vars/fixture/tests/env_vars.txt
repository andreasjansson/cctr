%platform unix
===
CCTR_WORK_DIR is set
===
test -n "$CCTR_WORK_DIR"
---

===
CCTR_WORK_DIR exists
===
test -d "$CCTR_WORK_DIR"
---

===
shell env vars work normally
===
echo "home=$HOME" | grep -o "home="
---
home=

===
CCTR_FIXTURE_DIR is set when fixture exists
===
test -n "$CCTR_FIXTURE_DIR"
---

===
can write to CCTR_WORK_DIR
===
echo "test" > "$CCTR_WORK_DIR/testfile.txt" && cat "$CCTR_WORK_DIR/testfile.txt"
---
test
